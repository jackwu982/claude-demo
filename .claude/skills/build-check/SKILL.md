---
name: build-check
description: Run the Vite build and fix any errors found
disable-model-invocation: true
---

Run `pnpm run build` in the project root. If the build fails, read the error output and fix the issues, then run the build again. Loop until the build passes or you cannot determine the fix.
